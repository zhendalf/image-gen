{
  "mcpServers": {
    "images": {
      "type": "stdio",
      "command": "bunx",
      "args": ["images-mcp"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}",
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
